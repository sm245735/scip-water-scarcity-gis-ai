
---

## 2026-04-12｜水庫 Comparison API 爬蟲（最重要）

### 背景
需要 2016-2026 的水庫歷史日資料作為 LSTM 訓練目標。嘗試多種方法：

### 失敗記錄

**1. WRA 新版 API（已關閉）**
- URL: `fhy.wra.gov.tw/WraApi/v1/Reservoir/DailyCondition`
- 結果: 404 / Request Rejected

**2. WRA 舊版 Statistics.aspx**
- 發現 CDP 可以操控，但**一次只查一天**
- 沒有 date range 功能，需要手動點擊 365 次
- Excel 下載按鈕綁定當天 postback，價值低

**3. opendata.wra.gov.tw API**
- 即時 API，有 Swagger 文件
- 只保留 1-2 天資料，**不是歷史資料庫**
- 建立每日收集 cron job，逐步建立歷史

**4. WRA 舊版 SOAP wsReservoir.asmx**
- `GetInfos` 方法只有當日水位
- 無歷史查詢功能

### 成功：Comparison API

**找到方式：**
1. 新網站 `fhy.wra.gov.tw/fhyv2/monitor/reservoir` 是一個 Vue 2 應用
2. 藏在 iframe 裡：`disaster/Reservoir/Comparison`（ASP.NET WebForms）
3. 點擊四個分頁時，iframe URL 不變（所以一開始以為沒東西）
4. 從 JS bundle URL 反向追蹤：`/Disaster/bundles/Comparision.js`
5. 在 JS 中找到關鍵程式碼：
```javascript
var n={ST_NO:ReservoirComparisonVM.checkedStations().join(","),
       StartDate:n.StartDate, EndDate:n.EndDate};
$.post(root+"/api/ReservoirHistoryApi/GetComparision", t, ...)
```

**API URL:** `POST https://fhy.wra.gov.tw/Disaster/api/ReservoirHistoryApi/GetComparision`

**請求參數:**
- `ST_NO`: 水庫 ID（最多 9 個，逗號分隔）
- `StartDate`: 開始日期（YYYY/MM/DD）
- `EndDate`: 結束日期（YYYY/MM/DD）

**關鍵限制：** API 只回傳「頭尾」各一筆記錄！
- 查詢 2023/01/01 ~ 2023/12/31 → 只回 2 筆
- 查詢 2023/01/01 ~ 2023/01/01 → 回 1 筆
- **策略：每次區間=1天**

**CDP 操控過程（參考 `cdp_wra.py`）：**
1. WebSocket handshake → CDP protocol
2. `Page.navigate` 導航到目標 URL
3. `Runtime.evaluate` 執行 JavaScript 操作 DOM
4. `Page.captureScreenshot` 截圖確認
5. 複雜互動：選項點擊 → 等 AJAX 回應 → 取結果

**CDP WebSocket 實現（純 Python 標準庫）：**
```python
def ws_handshake(sock, path):
    key = base64.b64encode(os.urandom(16)).decode()
    req = ("GET %s HTTP/1.1\r\nHost: %s:%d\r\n"
            "Upgrade: websocket\r\nConnection: Upgrade\r\n"
            "Sec-WebSocket-Key: %s\r\n"
            "Sec-WebSocket-Version: 13\r\n\r\n")
    sock.sendall(req.encode())
    resp = sock.recv(4096)  # 等待 101 Switching Protocols
```

### 最終收集策略

```bash
# 測試（10天）：
python 水庫歷史日資料收集_v1_20260412.py --start 2023/01/01 --end 2023/01/10 --days-per-batch 1

# 完整歷史（2016-2026，背景執行）：
python 水庫歷史日資料收集_v1_20260412.py --start 2016/01/01 --end 2026/04/12 --days-per-batch 10
```

**⚠️ 注意：寶山水庫（供應竹科）不在 Comparison API 中。**

---

## 2026-04-12｜rainfall_grid_data 表建立

**地點：** thesis_analysis 資料庫（PostgreSQL + PostGIS）

**用途：** 储存 TCCIP 網格化降雨資料，支援空間查詢

**SQL：**
```sql
CREATE TABLE rainfall_grid_data (
 id SERIAL PRIMARY KEY,
 area_name VARCHAR(50) NOT NULL,  -- '竹科', '中科', '南科'
 lon DOUBLE PRECISION NOT NULL,
 lat DOUBLE PRECISION NOT NULL,
 geom GEOMETRY(Point, 4326),
 data_date DATE NOT NULL,
 rainfall_mm NUMERIC(10, 2),
 created_at TIMESTAMP DEFAULT '2026-04-11',
 acquired_at TIMESTAMP DEFAULT '2026-04-11'
);

CREATE INDEX idx_rainfall_date ON rainfall_grid_data(data_date);
CREATE INDEX idx_rainfall_area ON rainfall_grid_data(area_name);
CREATE INDEX idx_rainfall_geom ON rainfall_grid_data USING GIST(geom);
```

**資料來源：** Google Drive → `自身論文/03. 資料/01. 氣候（TCCIP）/`
**本地路徑：** `/home/sm245735/.openclaw/workspace/workspace/03. 資料/01. 氣候（TCCIP）/`

**Python 批次匯入腳本：** `/home/sm245735/.openclaw/workspace/workspace/import_rainfall_tccip.py`
**執行環境：** `thesis_python_dev` Docker 容器


---

## 2026-04-12｜TCCIP 降雨資料匯入（進行中）

**目標：** 將 TCCIP 網格化降雨 CSV（新竹市 1960-2023）匯入 `rainfall_grid_data` 表

**問題根本原因：** 兩個獨立的問題
1. DB URL 中密碼含 `@`，需使用 `urllib.parse.quote_plus()` URL encode，否則 `${DB_PASSWORD}@db` 被錯誤解析為 host=`***@db`
2. PostGIS geometry 欄位名稱是 `geom`，但 `gpd.GeoDataFrame(geometry=...)` 預設產生的 geometry 欄位名稱是 `geometry`

**解法：** 
- 使用 `quote_plus(DB_PASS)` 處理密碼
- 使用 `gdf.rename_geometry('geom')` 而非 `rename(columns={'geometry': 'geom'})` 來改 geometry 欄位名稱（`rename_geometry` 會正確更新 GeoDataFrame 的 active geometry column）

**腳本位置：** `/home/sm245735/.openclaw/workspace/workspace/import_rainfall_tccip.py`
**執行環境：** `thesis_python_dev` Docker 容器
**container 內路徑：** `/app/import_rainfall_tccip.py`

**執行紀錄：**
- v1：失敗（Unnamed 欄位 date parse 失敗）→ 用 `usecols=lambda` 過濾
- v2：失敗（`geometry` 改名後 `['geom'] not in index`，因為 `rename(columns)` 不會改 active geometry name）
- v3：失敗（同上）
- v4：**成功** — 使用 `gdf.rename_geometry('geom')` 配合 URL encode 密碼，單一 CSV（2023）成功匯入 53,655 列（147 網格 × 365 天），驗證 `SELECT ST_AsText(geom)` 確認 Point geometry 正確

**成功標準驗證：**
- ✅ 單一 CSV（2023）成功匯入 53,655 列（147 網格 × 365 天）
- ✅ `SELECT ST_AsText(geom) FROM rainfall_grid_data LIMIT 3` 確認 Point(120.87 24.74) 等正確

---

## 2026-04-12｜水庫蓄水範圍 Shapefile 匯入

**資料來源：** Google Drive → `自身論文/03. 資料/02. 水資源（水庫蓄水）/`
**本地路徑：** `data/水資源（水庫蓄水）/ressub.shp`

**資料概況：**
- 來源：水利地理資訊服務平台
- 建置日期：2024-06-17
- 筆數：129 個水庫（蓄水範圍 polygon）
- CRS：EPSG:3826 (TWD97) → 已轉換為 EPSG:4326 (WGS84)
- 幾何型態：Polygon Z（3D）

**資料庫 Table：`reservoir_boundaries`**
```sql
CREATE TABLE reservoir_boundaries (
    id SERIAL PRIMARY KEY,
    res_name VARCHAR(100),
    area_description TEXT,
    source VARCHAR(200),
    build_date VARCHAR(20),
    geom GEOMETRY(GeometryZ, 4326),
    created_at TIMESTAMP DEFAULT '2026-04-12'
);
```

**匯入腳本：** `src/gis_analysis/水庫蓄水範圍匯入_v1_20260412.py`
**執行指令：**
```bash
docker exec thesis_python_dev python /app/src/gis_analysis/水庫蓄水範圍匯入_v1_20260412.py
```

**技術重點：**
- 原始 shapefile 幾何為 3D (PolygonZ)，需用 `ST_Force3DZ` 保留 Z 維度
- 使用 `ST_SetSRID(ST_Force3DZ(ST_GeomFromWKB(%s)), 4326)` 寫入
- 實驗證明：`to_postgis` 無法處理此 3D 幾何，改用 psycopg2 直接寫入

**驗證：**
```sql
SELECT res_name, ST_GeometryType(geom), COUNT(*)
FROM reservoir_boundaries GROUP BY res_name, ST_GeometryType(geom);
```

---

## 2026-04-12｜Spatial Join 與新竹縣市集水區每日降雨查詢

### 資料現況
| 資料表 | 筆數 | CRS | 說明 |
|--------|------|-----|-------|
| `rainfall_grid_data` | 大量（新竹/台中/台南） | EPSG:4326 | TCCIP 網格降雨點 |
| `reservoir_boundaries` | 129 | EPSG:4326 | 水庫蓄水範圍 |
| `reservoir_catchments` | 839 | EPSG:4326 | 集水區（由 EPSG:3826 轉換）|

### 關鍵 SQL（Spatial Join，學長提供）
```sql
SELECT
    c.basin_name,
    r.data_date,
    AVG(r.rainfall_mm) AS avg_catchment_rainfall
FROM rainfall_grid_data r
JOIN reservoir_catchments c
ON ST_Intersects(r.geom, c.geom)
WHERE r.data_date BETWEEN '2023-01-01' AND '2023-12-31'
GROUP BY c.basin_name, r.data_date
ORDER BY c.basin_name, r.data_date;
```

### 座標系統一致很重要！（學長提醒）
- `rainfall_grid_data`：EPSG:4326（WGS84）
- `reservoir_catchments`：EPSG:3826 → 已轉換成 EPSG:4326
- **若 CRS 不一致，`ST_Intersects` 會算出空結果！**

### 查詢腳本
- **新竹縣市限定版**（先用縣市界線 shapefile 限縮集水區）：`src/gis_analysis/新竹集水區每日降雨查詢_v1_20260412.py`
- 查詢結果已匯出：`data/新竹縣市_集水區每日降雨量_2023.csv`（3285筆，9個集水區）

### 驗證結果
- ✅ 成功查出 2023 年 9 個集水區的每日平均降雨量
- ✅ 中港溪 2023-01-01: 0.0mm, 2023-01-02: 1.4mm（合理）

---

### 程式檔案命名規則（研究實踐）
康康在研究過程中建立的命名慣例：**`程式名稱_v版本_YYYYMMDD.py`**
- 所有 `src/` 下的 Python 程式皆遵循此格式
- 範例：`集水區匯入_v1_20260412.py`、`水庫蓄水範圍匯入_v1_20260412.py`
- 詳細規則見 `SOP-GitHub操作.md`

---

## 2026-04-12｜TCCIP 竹科降雨資料匯出（2019-2023）

### tccip_daily_rainfall.csv 發現
- 來源：TCCIP 觀測日資料（已預先處理好的版本）
- 欄位：`date`, `park`（竹科/中科/南科）, `city`, `lon`, `lat`, `rainfall_mm`
- 覆蓋：2019-01-01 ~ 2023-12-31，共 5478 筆（3個園區平均）
- **竹科单独：1826 筆**（完整5年）

### 為什麼用竹科，而不是 Basin-level？
- 水庫日資料 API 還沒確認（fhy.wra.gov.tw 404）
- tccip_daily_rainfall.csv 的「竹科」是定點觀測，代表竹科園區附近的降雨
- 寶山水庫供應竹科，用竹科降雨当作 Feature 是合理的代理指標
- 以後有水庫日資料後可以替換，月資料 merge 留到最後一步

### 輸出欄位
```csv
date, rainfall_mm_zhuke
2019-01-01, 0.00
2019-01-02, 0.00
2019-01-03, 2.31
```

---

## 2026-04-12｜Statistics.aspx Selenium 自動化（重大突破）

### 問題背景
- WRA Statistics.aspx 是 ASP.NET WebForms，每個下拉選單改變都會 PostBack
- CDP WebSocket 控制 ASP.NET 時，每次 PostBack 都断线（`BrokenPipe` / `Connection reset by peer`）
- 直接 `requests` POST 被 WAF 擋（Request Rejected）
- Comparison API 無法取得寶山水庫（已被水利署從防汛重點移除）

### 解決方案：Selenium Remote Debugging

**關鍵發現：**
- 水利署統計頁需選擇「全部」模式才能看到 113 個水庫（含寶山水庫）
- 預設「防汛重點水庫」只有 20 個，沒有寶山水庫

**實作步驟：**
1. Chrome 已在主機以 `--remote-debugging-port=18800` 開啟（不要另外開）
2. Selenium 串上 `http://127.0.0.1:18800`，不需要容器內的 Chrome
3. ASP.NET 下拉選單用 `execute_script` 執行 JS 點擊繞過 PostBack：
   ```python
   driver.execute_script("""
       var y = document.getElementById('ctl00_cphMain_ucDate_cboYear');
       if(y){y.value='2026'; y.dispatchEvent(new Event('change', {bubbles:true}));}
   """)
   ```
4. 表格讀取用 JS 直接 DOM access：
   ```python
   driver.execute_script("""
       var tables = document.querySelectorAll('table');
       var rows = tables[0].querySelectorAll('tr');
       var data = [];
       for(var r of rows) {
           var c = r.querySelectorAll('td');
           if(c.length > 0) data.push(Array.from(c).map(t=>t.innerText.trim()));
       }
       return JSON.stringify(data);
   """)
   ```

**本機 Selenium 環境：**
- Python venv：`~/selenium_env`
- 安裝：`python3 -m venv ~/selenium_env && source ~/selenium_env/bin/activate && pip install selenium`
- 執行：`python3 水庫Statistics_全量收集.py`（本機執行，不是 Docker）

### 欄位單位對照（已確認）

| 欄位 | 說明 | 單位 |
|------|------|------|
| `date` | 查詢日期 | YYYY-MM-DD |
| `reservoir_name` | 水庫名稱 | - |
| `observation_time` | 水情時間（觀測時間）| YYYY-MM-DD HH:MM:SS |
| `basin_rainfall_mm` | 本日集水區累積降雨 | mm（公厘）|
| `inflow_cms` | 進流量（總入流）| cms（立方公尺/秒）|
| `water_level_m` | 水位（觀測水位）| m（公尺）|
| `full_water_level_m` | 滿水位 | m（公尺）|
| `effective_storage` | 有效蓄水量 | 萬立方公尺 |
| `storage_rate` | 蓄水百分比 | %（百分比）|
| `outflow_cms` | 水庫出流量（小計）| cms（立方公尺/秒）|

### 收集狀態（2026-04-12 22:38 啟動）
- 腳本：`src/data_pipeline/水庫Statistics_全量收集.py`
- 後台 PID：4038785
- 範圍：2016-01-01 至 2026-01-01（3654 天）
- 水庫：113 個（含寶山水庫 10201）
- 預估：412,902 筆資料，18-20 小時完成
- 輸出：`data/水庫歷史水情/水庫統計表_2016_2026.csv`
- Log：`logs/statistics_run.log`

### 後續處理（跑完後）
1. 補上 CSV header（date, reservoir_name, observation_time, ...）
2. 更新 `database/schema.sql` 的 `reservoir_daily` 表 column comments
3. 將 CSV 匯入 PostgreSQL `reservoir_daily` 表


---

## ⚠️ 待修正：集水區降雨量應用面積加權平均

### 現況
目前 `新竹縣市_集水區每日降雨量_2023.csv` 是用 `ST_Intersects` 簡單平均，**每個 TCCIP 網格被視為同等權重**。

### 問題
TCCIP 網格大小為 0.01° × 0.01°，每個網格只佔集水區的一部分面積。簡單平均會讓邊緣網格（只覆蓋集水區一小塊區域）和中心網格有一樣的權重，**不夠精確**。

### 解決方案
用 `ST_Intersection` 計算每個 TCCIP 網格與集水區的**交集幾何（Polygon）**，再用**交集面積**做加權平均：

```sql
SELECT
    basin_name,
    data_date,
    SUM(intersection_area * rainfall_mm) / SUM(intersection_area) AS weighted_avg_rainfall
FROM (
    SELECT
        b.basin_name,
        t.data_date,
        t.rainfall_mm,
        ST_Area(ST_Intersection(t.geom::geography, b.geom::geography)) AS intersection_area
    FROM rainfall_grid_data t
    JOIN reservoir_catchments b ON ST_Intersects(t.geom, b.geom)
    WHERE b.basin_name = '頭前溪'
) sub
GROUP BY basin_name, data_date
```

### 待辦
- [ ] 等水庫統計資料收集完成（今天下午）
- [ ] 重新計算 2016-2023 年集水區降雨（面積加權版）
- [ ] 更新 CSV 輸出並 commit


---

## ⚠️ 待補充：氣象資料缺口（氣溫、蒸發量）

### 需求背景
水庫水量平衡需要蒸發量直接估算；氣溫影響蒸發速率。

| 要素 | 對水庫蓄水量影響 | 可能來源 |
|------|----------------|---------|
| **氣溫** | 高溫增加蒸發散量 | 中央氣象署網格化日均溫、TCCIP |
| **蒸發量** | 直接減少水庫蓄水量 | 水利署水庫蒸發量觀測資料 |

### 待確認資料來源

#### 氣溫
- [ ] **中央氣象署（CWB）開放資料平台**：網格化日均溫（格點資料）
  - 網址：https://opendata.cwa.gov.tw/
  - 需確認是否有 TCCIP 以外的網格化溫度產品
- [ ] **TCCIP**：檢查是否有其他氣象要素（部分 TCCIP 資料含均溫）
- [ ] **水利署地面水觀測站**：雨量站通常也有氣溫觀測，可空間內插至水庫位置

#### 蒸發量
- [ ] **水利署水文情勢網**：水庫蒸發量觀測（月統計）
  - 網址：https://wuss.wra.gov.tw/
- [ ] **CWB 氣象站蒸發量資料**：各氣象站有直徑 20cm 蒸發皿觀測
- [ ] **TCCIP**：部分網格化產品含蒸發量估算
- [ ] **實務估算**：若無直接觀測，可用 Hargreaves 公式估算（需氣溫 + 濕度）
  ```
  ETo = 0.0023 × (T + 17.8) × T^0.5 × (Ra - 0.0)
  ```

### 待辦（氣象資料）
- [ ] 確認 CWB 開放資料平台是否有網格化日均溫可下載
- [ ] 確認水利署水庫蒸發量觀測資料是否對外開放
- [ ] 評估 Hargreaves 蒸發量估算公式的可行性（若無直接觀測）
- [ ] 確認資料時間範圍需覆蓋 2016-2023


---

## 水庫出水/供水量（2026-04-13 更新）

### 寶二水庫出水量
- ✅ Statistics.aspx 有 `outflow_cms`（出水量，立方公尺/秒）
- 但許多天是空白（`--`），並非每天都有觀測值
- **需注意**：這個「出水量」是水庫的**總出水量**，不等於「供竹科水量」（可能有一部分流向下游農業/民生）

### 寶二→竹科供水資料
- 屬於「台灣自來水公司第三區管理處」管轄
- 公開資料中難以取得寶二對竹科的每日實際供水量
- 月供水資料可能可在 EIA（能源局）或自來水公司年報找到

### 竹科園區用水量
- ❌ 日用水量：拿不到（科學園區管理局內部資料，未對外開放）
- △ 月用水量：EIA 能源局有統計「科學園區用水量」，但僅有月統計值
- 若需要日尺度用水量，通常用「月均攤平」或「依季節係數估算」


---

## 📋 待辦：科學園區月用水量資料蒐集

### 需求
- 目的：用月均線性內插估計日用水量，作為 LSTM 模型的額外 Feature
- 適用：水庫出水/供水 Loss 估算，或作為模型驗證對照組

### 待確認資料來源
| 來源 | 說明 |
|------|------|
| 經濟部能源局（EIA）| 「電力與用水統計」有科學園區月用水量 |
| 科學園區管理局 | 園區統計年報，月/季度用水量 |
| 台灣自來水公司 | 第三區處（供水新竹）可能有月統計 |

### 待辦
- [ ] 確認 EIA 能源局是否有科學園區用水量月資料可下載
- [ ] 向科學園區管理局申請或找公開年報
- [ ] 確認資料時間範圍（需與 LSTM 訓練期 2016-2023 對齊）
- [ ] 取得月資料後，用線性內插擴展為日尺度用水量估算


---

## 2026-04-13｜頭前溪集水區降雨量查詢（ST_Union 修復 + COPY 方法）

### 問題根本原因
頭前溪（basin_id=1300）在 `reservoir_catchments` 表中有 **16 筆記錄**（多碎片幾何）。
直接使用 `WHERE basin_name = '頭前溪'` 會讓 ST_Intersects 比對**每一個碎片**與 TCCIP POINT，
造成 **False Negative**（0 筆結果）。

### 解法：ST_Union + COPY
```sql
WITH touqian AS (
    SELECT ST_Union(geom)::geometry as geom
    FROM reservoir_catchments
    WHERE basin_name = '頭前溪'
)
SELECT d::date, COALESCE(AVG(r.rainfall_mm), 0), COUNT(r.lon)
FROM generate_series('2016-01-01', '2023-12-31', '1 day') d
LEFT JOIN rainfall_grid_data r ON r.data_date = d::date
    AND ST_Intersects(r.geom, (SELECT geom FROM touqian))
GROUP BY d::date
ORDER BY d::date
```

**用 COPY 直接輸出 CSV**（避免 Python 迴圈 + 記憶體問題）：
```python
cur.copy_expert(sql, f)  # 完全在 DB 端處理，輸出到容器檔案
```

### 結果
- 43 個 TCCIP 網格位於頭前溪集水區（LON: 120.92-121.03, LAT: 24.76-24.85）
- 2016-01-01 ~ 2023-12-31，共 **2922 天**（包含 header 共 2923 行）
- 已上傳至 GitHub：`data_samples/新竹頭前溪集水區每日降雨量_2016_2023.csv`

### 關鍵程式
- `src/gis_analysis/頭前溪集水區降雨_SpatialJoin_v5_20260413.py`（最終失敗，但邏輯正確）
- 直接在 container 內用 COPY 方法成功

---

## 📋 研究範圍界定：為何只預測寶二水庫（不擴展至其他水庫）

### 發現
新竹科學園區（竹科）並非只依賴單一水庫供水，而是同時監控以下水庫進行調度：

| 水庫 | 說明 |
|------|------|
| **石門水庫** | 桃園/大台北，跨區調度支援新竹 |
| **寶山水庫** | 新竹境內，蓄水量較小 |
| **寶山第二水庫** | 新竹境內，蓄水量最大（3,100萬噸），**主要水源** |
| **鯉魚潭水庫** | 苗栗/台中，跨區調度支援新竹 |
| **永和山水庫** | 新竹/苗栗，區域備援 |

### 為何不納入模型
1. **維度過多**：5個水庫 × 多個Feature = 模型複雜度大幅上升
2. **人為調度變數**：水庫間的調度是政策性/人為決策，非純自然徎的時間序列
3. **研究重點明確**：本研究聚焦於「降雨 → 水庫蓄水量」的自然水文關係，而非水利署的供水調度策略

### 結論
本研究以**寶山第二水庫**為新竹地區供水風險指標，未來可擴展至多水庫建模，但需先建立單一水庫的 baseline 模型。

